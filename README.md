CORS proxy server modified and greatly simplified from [http-proxy](https://github.com/http-party/node-http-proxy) and [cors-anywhere](https://github.com/Rob--W/cors-anywhere/).

Start server with `yarn server`.

Start client with `yarn dev`.
